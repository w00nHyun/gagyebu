<nav class="navbar navbar-expand navbar-light bg-white border-bottom sticky-top py-3">
  <div class="custom-nav-wrapper d-flex align-items-center justify-content-between w-100">

    <!-- 로고 영역 -->
    <a class="navbar-brand d-flex align-items-center fs-5 text-dark m-0 p-0" href="/">
      <span class="me-2" style="color: #ff7e36;">👛</span>
      <strong class="fw-bold">가계부</strong>
    </a>

    <!-- 우측 사용자 상태 영역 -->
    <div class="d-flex align-items-center gap-3">
      <% if (user) { %>
        <!-- 1. 로그인 상태: 유저 이름 + 로그아웃 버튼 -->
        <span class="fw-semibold text-dark fs-6">
          👤 <strong class="text-primary">
            <%= user.username %>
          </strong>님
        </span>
        <form action="/logout" method="POST" class="d-inline">
          <button type="submit"
            class="logout-btn d-flex align-items-center justify-content-center text-decoration-none border-0 bg-transparent p-0">
            <span>로그아웃</span>
          </button>
        </form>
        <% } else { %>
          <!-- 2. 비로그인 상태: 로그인 버튼 -->
          <a href="/login" class="login-btn d-flex align-items-center justify-content-center text-decoration-none">
            <span class="me-2">🔐</span>
            <span>로그인</span>
          </a>
          <% } %>
    </div>

  </div>
</nav>
<style>
  nav.navbar .custom-nav-wrapper {
    width: 100%;
    max-width: 1120px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 20px;
    padding-right: 20px;
    box-sizing: border-box;
  }

  nav.navbar .login-btn {
    padding: 8px 16px;
    border-radius: 999px;
    background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
    color: white;
    font-size: 14px;
    font-weight: 600;
    box-shadow: 0 2px 8px rgba(30, 64, 175, 0.2);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    min-width: 92px;
    height: 40px;
  }

  nav.navbar .login-btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(30, 64, 175, 0.25);
    color: white;
  }

  @media (min-width: 1200px) {
    nav.navbar .custom-nav-wrapper {
      padding-left: 24px;
      padding-right: 24px;
    }
  }

  @media (max-width: 768px) {
    nav.navbar .login-btn {
      padding: 7px 12px;
      min-width: 80px;
      height: 36px;
      font-size: 13px;
    }
  }
</style>